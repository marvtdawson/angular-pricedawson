let express = require('express');
let MongoClient = require('mongodb').MongoClient;

console.log('Connected to App Server!');

//MongoClient.createCollection('pric3dAwson');

/*MongoClient.connect('mongodb://localhost:27017/pric3dAwson', function (err, db) {
  db.collection('contactus').find().toArray(function (err, result) {
    if (err) throw err;

    console.log(result)
  })
});*/
